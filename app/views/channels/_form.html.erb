<%= simple_form_for(@channel) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :course_id %>
    <%= f.input :belongs_to %>
    <%= f.input :has_one %>
    <%= f.input :has_one %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
