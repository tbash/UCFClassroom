<%= simple_form_for(@messenge) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :sender %>
    <%= f.input :content %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
